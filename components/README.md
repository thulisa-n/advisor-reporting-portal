# Components Folder Notes

This folder is reserved for cross-project UI component documentation and design references.

For this MVP implementation, active UI components are currently authored in `src/App.tsx` for speed and demo readiness.

Planned extraction path after demo:
- `src/components/layout/*`
- `src/components/forms/*`
- `src/components/report/*`
